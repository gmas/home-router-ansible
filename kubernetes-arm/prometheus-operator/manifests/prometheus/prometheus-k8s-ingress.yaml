apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: prometheus
  namespace: monitoring
spec:
  rules:
  - host: prometheus.k8s.home.masgras.us
    http:
      paths:
      - path: /
        backend:
          serviceName: prometheus-k8s
          servicePort: web
