# saves IP as gateway in (older,deprecated?) dhcpcd client format
# used by Shorewall to 'detect' provider gateway
echo "GATEWAYS=$new_routers" > /var/lib/dhcpcd/dhcpcd-eth1.info
ip route del default via $new_routers dev eth1
